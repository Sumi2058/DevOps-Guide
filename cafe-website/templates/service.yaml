apiVersion: v1
kind: Service
metadata:
  name: {{ include "cafe-website.fullname" . }}
  labels:
    {{- include "cafe-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cafe-website.selectorLabels" . | nindent 4 }}
